@model IEnumerable<_6351071005_LTWEB_K63.Models.LOAIXE>
<p class="lead"> LOAI XE</p>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheoloaixe", "Home", new { id = item.MaLX})" class="list-group-item">
            @item.TenLoaiXe
        </a>
    }
</div>
